# ``UUIDs``

@Metadata {
    @DisplayName("UUIDs")
    @TitleHeading("Swift Foundations")
}

L3 unifier composing the RFC 4122 / RFC 9562 spec encoding with the platform
CSPRNG. Importing `UUIDs` brings the canonical typed UUID and spec-correct
v3 / v4 / v5 / v7 / v8 generators on every supported platform.

## Topics
